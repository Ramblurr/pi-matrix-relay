# Broker

JVM Clojure Matrix broker.

## Local dev

Run from `broker/`:

- `bb dev` — start nREPL/dev.
- `bb broker` — run broker foreground.
- `bb lint` — run clj-kondo.
- `bb test` — run JVM tests.
- `bb fmt` / `bb fmt:check` — format/check code.
